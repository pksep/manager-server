# Фаза 2: транспорт обращений и контакты ЕРП

Задача и ветки: `manager`. Состояние на 11.09.2026: готовы транспорт сообщений
и синхронизация контактов, вся фаза ещё не завершена.

Дополнение 15.09.2026: раздел «Клиенты» и права `chat.clients:view` работают
через настоящую ERP; локальный стенд запускает полный AppModule. Добавлены
RabbitMQ, Sequelize/Umzug и защита публичного приёма. Текущее поведение и условия
запуска описаны в [SECURITY.md](SECURITY.md). Ниже сохранён исторический срез фазы;
старые команды с отладочными скриптами ERP не входят в поставку и не являются
текущим способом запуска.

## Что можно проверить сейчас

Посетитель открывает виджет, оставляет контакты и отправляет текст или файл.
Сервис сохраняет обращение и очередь доставки в своей PostgreSQL. Сервер чата
создаёт обычную группу через GroupsService и хранит сообщения через
MessagesService, вложения через Media и S3Service. Менеджер отвечает в тот же
топик; посетитель получает ответ и отметки прочтения по WebSocket.

Первый ответ назначает ответственного. Другой менеджер может ответить, не
меняя его. Ручное назначение сохраняется даже при запоздалом получении первого
ответа. Источник сайта фиксируется серверной конфигурацией, query/hash страницы
не сохраняются. Новая гостевая сессия не получает прежние обращения.

Повторы защищены уникальными ограничениями и блокировками базы. На стороне
чата сообщение, его Media, подтверждение операции и событие фиксируются одной
транзакцией. На стороне manager приём сообщения и операция доставки также
атомарны. После потерянного ответа либо перезапуска повтор использует прежний
идентификатор; второй топик/сообщение не создаётся.

## Фактические контракты

| Участок | Контракт |
| --- | --- |
| Виджет → manager | `/v1/widget/session`, `/inquiries`, `/inquiries/:id/messages`, `/attachments`, `/attachments/:id` |
| Обновления виджета | `/v1/widget/events`; первое сообщение `authenticate`, затем `ready`, `message`, `ping`/`pong` |
| manager → чат | `/api/internal/manager/inquiries/:id/messages`, `/events?after=…`, `/attachments`, `/attachments/:id`, `/managers/:id/access` |
| Сотрудник → чат | `/api/manager/inquiries`, `/inquiries/:id`, `/inquiries/:id/messages`, `/attachments`, `/assignee`, `/retry` |
| Чат → manager | `/internal/staff/inquiries`, `/inquiries/:id`, `/assignee`, `/retry`; заголовки `x-manager-key`, `x-actor-id` |
| Снимок полномочий | `PUT /api/internal/manager-access/snapshot`; отдельный `x-manager-access-key`, монотонная revision, срок не более 15 минут |

Последние три кратких маршрута каждого раздела являются дочерними относительно
`/inquiries/:id`. Схемы публичного протокола и проверка событий находятся в
`src/contracts.ts`; сервер чата проверяет собственный вход через Zod. Совместимость
между двумя репозиториями проверяет настоящий HTTP-тест, включая разбор каждого
события схемой manager. Новая внутренняя версия — `1`.

Событие идентифицируется парой `(inquiryId, sequence)`, где inquiryId уже задан
маршрутом. `messageId` относится к каноническому сообщению чата. Для исходящего
сообщения `operationId` связывает его с сохранённым сообщением manager.
Типы событий этого участка — `message` и `read`. Курсор продвигается только
при непрерывной последовательности. Собственные исходящие события обновляют
подтверждение, не создавая входящее сообщение.

Получение ответов пока выполнено опросом долговечного журнала чата. RabbitMQ
сохраняет существующую работу чата, но новая доставка не полагается на его
недолговечное уведомление. Интервал задаёт `MANAGER_WORKER_MS`; один проход
обрабатывает до 20 отправок и 50 обращений, страницы чередуются. Доставка
повторяется до 10 попыток с задержкой до 60 секунд. После этого видна ошибка
в карточке; сотрудник может повторить доставку с тем же ключом.

## Доступ и файлы

Гостевой токен хранится только как SHA-256 и ограничен сроком, сайтом и origin
виджета. Отдельно проверяется origin исходного сайта. Внешний автор чата имеет
`is_external=true`, исключён из справочника сотрудников и вариантов входа;
обычный вход и сокетная авторизация для него запрещены.

Право раздела проверяется при чтении, ответе, назначении и скачивании. Просроченный
либо отсутствующий снимок закрывает менеджерский доступ. Снимок обновляет штатные
TopicSetting/GroupMemberRole. Отозванный пользователь исключается из топиков,
списков и повторной синхронизации. Полные сообщения внешних обращений рассылаются
только личным комнатам менеджеров с действующим правом.

Файлы хранятся в существующем зашифрованном S3 чата, в области `manager-private/`.
Посетитель использует attachmentId своей сессии; произвольный objectPath от него
не принимается. Общая выдача `/api/media/object/…` тоже проверяет право сотрудника
и принадлежность файлу обращения. Файлы выдаются как скачивание, с `no-store`
и `nosniff`. Клиент чата загружает их с авторизацией и не кладёт в общий IndexedDB
кеш. Служебные ключи скрыты в HTTP-журнале.

Сейчас загрузка целого файла ограничена конфигурацией сайта, максимумом 100 MiB
на сервере и числом вложений; локально — 10 MiB и 5 файлов. Повтор загрузки с
тем же ключом возвращает тот же файл, а другое содержимое отклоняется. Русские
имена сохраняются. Ответы с файлами используют специальный загрузчик обращения
через `/api/manager/inquiries/:id/attachments`.

## Что ещё не завершено

- Подтверждение владения телефоном/почтой. По уточнению от 14.09.2026 совпадение
  обоих нормализованных реквизитов связывает клиента и продолжает его открытый
  чат автоматически; это не даёт посетителю доступ к истории других сессий.
- Производитель снимка прав из ЕРП и редактор ролей; это зависимость
  этапа 3. На изолированном стенде снимок создают только тестовые сценарии.
- Новый раздел и действия в интерфейсе чата, подключение его редактора к
  защищённому загрузчику. Отправка старым общим загрузчиком в специальный топик
  явно отклоняется; обычные чаты продолжают работать как раньше.
- Очистка несвязанных загрузок, возобновляемая передача, защищённые превью
  изображений/видео, политика MIME и производственные квоты.
- Полная постраничная история в интерфейсах, обслуживание срока хранения,
  метрики очереди, нагрузочные проверки и проверка нескольких экземпляров.
  Сейчас снимок виджета содержит последние 500 сообщений текущей сессии;
  HTTP-история доступна страницами по 100, список обращений ограничен 100.
- Изменение, удаление и пересылка сообщений внешнего обращения. Сейчас эти
  операции отклоняются явно, до добавления соответствующих событий в виджет.
- Реальные адаптеры других платформ. Текущий вход принимает только `widget`;
  ссылки на социальные сети не выданы за интеграцию переписки.

## Проверки

`bun run test:integration` использует отдельные базы `manager_local` и
`manager_chat_local`, настоящий сервер СЭП Чата, Redis, RabbitMQ и S3. Внешние
производственные подключения тест запрещает. Семь сценариев проверяют:
источник/авторизацию; пять одновременных повторов; ответы/прочтения/назначение;
файлы в обе стороны и чужой доступ; новую сессию при совпавших контактах;
потерю подтверждения и перезапуск; отзыв права и запрет входа посетителя.

`bun run test:browser` запускает Chrome: контактная форма → настоящее обращение →
ответ из чата → файл без модального окна → полное скрытие при остановке manager.
Снимок проверки: `.local/widget-real-chat.png`. Браузерный драйвер запускается
через Node: в этом Windows-окружении запуск Playwright напрямую через Bun зависал
на создании страницы. Сам manager отдельно проверен при выполнении в Bun.

Дополнительно пройдены сборки manager, чата и виджета, проверка типов клиента
чата и 70 существующих тестов сообщений, групп, топиков, приглашений и файлов.
Это проверка первого участка второй фазы, а не приёмка всей интеграции с ЕРП
или разрешение выпускать её на производственный сайт.

## Синхронизация контактов (второй участок)

Менеджер получает кандидатов через `GET /api/manager/inquiries/:id/erp/candidates`.
Поиск учитывает точные телефон/email, регистр email, форматирование телефона,
архивные записи; возвращает до 21 кандидата. Совпадение не доказывает личность.
`POST .../erp/sync` принимает `{operationId, contactId?}`. Без contactId создаётся
новый контакт, только если совпадений нет; с contactId связывается явно выбранный
активный контакт. Его имя и реквизиты не перезаписываются.

Операция сначала сохраняется в manager и выполняется отдельной очередью. Повтор
сохраняет ключ, параметры и инициатора. Состояния: pending, working, completed,
conflict, failed; до 10 попыток, задержка до 60 секунд. Карточка возвращает
`erpOperations`. После конфликта менеджер выбирает контакт и отправляет новую
команду; `POST .../erp/retry {operationId}` возобновляет failed с прежним ключом.
Если права исходного инициатора отозваны, его операция не выполняется от имени
другого сотрудника. Повтор failed запрещён при другой активной/завершённой операции.

manager обращается к `POST /api/internal/manager/contacts/candidates|sync` ЕРП
с отдельным `ERP_MANAGER_KEY` и `x-erp-actor-id`. DTO размещены в
`@pksep/zod-shared`, контроллеры тонкие; ContactService создаёт контакт и штатное
действие через ActionsService в транзакции обёртки. Журнал повтора и связь клиента
фиксируются в той же транзакции. AfterCommit ActionsChain остаётся штатным.
Повтор после потерянного ответа возвращает сохранённый результат.

Подтверждённый ERP actor приходит из `erpActors: [{userId, erpUserId}]` снимка
прав чата. `User.ex` не используется для этого решения. Старый снимок без
erpActors сохраняет работу переписки, но не разрешает синхронизацию. ЕРП заново
проверяет действующие User/Role и `manager.read`; создание дополнительно требует
`contactBase.changeEverything`, как штатная кнопка базы контактов. Отсутствующие
права означают запрет. Автоматического расширения старых ролей нет.

`GET /api/manager/inquiries/:id/history?after=0` возвращает до 100 сообщений и
nextCursor. Менеджер видит обращения карточек, явно связанных с одним контактом
ЕРП. Связи исходных топиков не меняются. Гостевые API по-прежнему ограничены одной
сессией и не раскрывают другую переписку даже после синхронизации.

Локально: `bun run local:erp` готовит отдельную manager_erp_local и закрытый env;
сервис ЕРП для проверки запускается из ветки manager командой
`node --env-file=env/.manager.env -r tsconfig-paths/register .local/manager-local-server.cjs`
с `TS_NODE_BASEURL=dist`, после build:lib/build. Это стенд настоящих моделей,
ContactService/ActionsService, guard и middleware, а не запуск всех модулей ЕРП.
Производственные afterSync-представления здесь не строятся, исторические миграции
не переигрываются. CRUD/afterCommit hooks работают штатно.

`bun run test:erp` проверяет настоящий HTTP-контракт и базы всех трёх сервисов:
защиту доступа, конкуренцию, выбор/архив, откат, журнал инициатора, общую историю
менеджера и изоляцию гостя, потерю ответа с перезапуском, переписку при отказе ЕРП.
Исторически локальный `.local/manager-migrations-check.cjs` проверял up / повтор up / down / up в
manager_erp_migrations_local на том же изолированном PostgreSQL; ожидающих — 0.
Полная приёмка развёртывания всей ЕРП и производство снимков прав остаются впереди.

Итог проверок этого участка: 10 сквозных сценариев ЕРП (86 проверок), 7 сценариев
транспорта (84 проверки), 3 модульных теста manager, 9 тестов middleware ЕРП.
Сборки manager, chat_server и sep_erp_server, профильный ESLint и проверки
миграций прошли. Стенд обнаружил и помог исправить несовместимый синтаксис
именованных SQL-параметров; в окончательном прогоне все сценарии успешны.

Фиксация в Git: manager-server и chat_server отправлены в ветки manager.
Изменения ЕРП пока подготовлены локально: pre-commit блокируют два существующих
сбоя полного набора (AI-каталог отчётов и поток ошибки Excel). Оба воспроизведены
на неизменённом canary 6a10b9694. Полный прогон: 1201 passed, 2 failed, 14 skipped;
профильные проверки manager проходят. Проверка не обходилась. Поэтому весь
набор изменений пока нельзя считать полностью отправленным или готовым к выпуску.

14.09.2026: после разрешения продолжить коммит ЕРП `67aaa62ca` отправлен в
`sep_erp_server:manager` с разовым исключением pre-commit. Это снимает только
блокер отправки: два исходных сбоя тестов не исправлялись. Проводится новый
полный локальный прогон окончательных серверных версий с одним тестовым
процессом на репозиторий и отдельной проверкой интеграций manager. Готовность
к слиянию или развёртыванию до результатов этой проверки не объявляется.

15.09.2026: ветки manager в ERP client/server объединены с актуальным canary.
Изолированный интерфейс 4315 теперь работает с полной точкой входа ERP AppModule
на 4503, обычной авторизацией, отдельными Neo4j, Redis DB 13, MinIO bucket
manager-erp и RabbitMQ vhost manager-erp. Исходная тестовая база и контакты сохранены;
до миграции сделан локальный pg_dump. Фоновые ERP workers и внешние события отключены.

Право менеджера перенесено в штатные роли «Чат → Клиенты → Полный доступ».
Chat server получает его непосредственно из ERP, включая отзыв, блокировку
и срок действия снимка 30 секунд. Служебные запросы к полному ERP отключают
прикладное сжатие ответов заголовком compress: no-compress.

Проверки: полный chat server 323 passed / 0 failed / 0 skipped; полный ERP unit
1411 passed / 1 failed / 14 skipped. Остался сбой ResultWorkExcelExportHandler
в result-work-excel-export.handler.spec.ts. После последней правки middleware
повторены оба профильных набора: 17 passed / 0 failed / 0 skipped.
В браузере проверены штатный вход, база контактов, роли, один переключатель,
отзыв/возврат менеджера в открытом чате; полный сценарий verify-erp-ui.mjs
прошёл (редактирование, предзаполнение, отмена, валидация, создание и повторное
редактирование одного контакта, обычная карточка; без JS/HTTP ошибок).
Для полного ERP этот скрипт принимает MANAGER_ERP_LOGIN_FILE — путь к локальному
JSON с тестовыми tabel/password. Секретный файл хранится вне Git.
Это тестовый стенд, не подтверждение готовности всего ERP к выпуску.
